# 00 · 讨论记录与确认边界

日期：2026-09-15。以下来自本次对话；推荐实现方式与用户已确认范围分别记录。

## 用户确认

- improve-1～5.5 已实施；improve-6 处理新消息格式下的 Context 占用与压缩链路适配。
- “建议旧 chat 退出（可安全删除）但是需要测试验收我们自己研发的消息结构的可靠性”。结合用户同时要求三协议兼容，这里指内部旧 Chat 中间格式，保留 Chat Completions 协议支持。
- “自有结构既要表达通用内容，也要留出保存各协议特殊数据的位置”。
- “本批次不做压缩/prune算法的更改”，若已有问题影响占用统计或上下文语义传递，允许最小修复。
- 重新详细调查 `docs/core/context/improve-1`、`improve-2`、`improve-3`、`improve-4`、`improve-4-to-5-regression`、`improve-4.1`、`improve-5`、`improve-6`，让本次改动可追溯。
- 对比 `/Users/hansun025/Projects/code-cli/` 下七个指定项目；压缩／prune 新算法借鉴留待后续。
- 遵循 SWE 原则，不做过度设计。
- 先调研、优化并更新文档，再启动子代理审查；用户审核后才实施。
- 文档只需文件级范围，不额外列符号和行号改动清单。

此前用户选择“允许整理计量投影”，表示不必为保持旧数字继续使用旧 Chat 形状。这不等于已批准新增计量服务、预算公式或缓存体系。

## 推荐方案，供用户整体审核

直接完善已有 `ModelMessage`／`ModelToolCall`／`ModelState`，让 estimator 从同一份 `PreparedModelRequest` 中选择计量材料；三协议生产转换测试证明信息没有丢失、错配或重复。暂不增加三个协议专用计量器，也不按各协议 HTTP JSON 字符数追求一致。

这项具体选择已通过异步问题向用户征询；本版按推荐方案起草，**不写成用户已经确认**。它保留现有启发式与校准，承认不同协议包装和不透明原生数据带来的估算误差。

## 本轮不扩大

保留现行压缩阈值、选段、prune/mask 评分材料、摘要累计与 overflow 保留规则；不改输出预留公式，不增加计量版本／多维校准缓存／占用 dirty 状态，不重命名整套公开用量字段。摘要继续继承所属代理的 reasoning 配置。

该轮讨论的授权范围是调研、文档及子代理审查。后续实施授权见下节。

## 实施授权与最新校准（2026-09-15～16）

用户审核后要求开始实施：创建本地临时分支；每批单元、集成、真实 API／模型 E2E，子代理审查后分批 commit；不 merge/push。测试模型见用户提供的 `tests/models-4-tests.md`，凭证从 `.env` 读取。

继续采用推荐的单一自有请求 estimator；接受删除旧格式后估算数字变化，以新材料更新预期并解释差异，禁止调整阈值来凑旧数字。真实压缩必须有真实摘要请求和持久化证据，不能将 mock、skipped 或失败记为通过。

用户特别校准：占用率分母使用请求得到的模型完整上下文窗口；自动压缩在占用率达到 95% 时触发。当前代码按 input budget 计算 95% 与此不一致，应最小修正。窗口探测发生在模型连接时，既有检测优先／配置和默认回退链路保留，不能改用测试固定窗口冒充检测值。

实施时以用户最新明确的完整窗口 95% 要求为准，取消剩余输入预算 <4096 的提前摘要条件，保留上游 overflow 恢复。曾就 4096 条件另行征询但未收到单独答复；这是对最新要求的实现选择，不记录为额外用户确认。mask／thrash 的输入预算口径不变。
