# 讨论记录与已确认要点

> 2026-09-18 与用户讨论定稿。正式方案见 01–04。
> 来源：density 落地后的 Web chrome 跟进讨论。文档与上一批分开。

---

## 1. 背景与动机

会话进行中，工具卡、输入框、顶栏三处看起来发飘：箭头忽大忽小、字不在框中间、顶栏占的空白太多。都是展示层，不改发消息或工具执行。

## 2. 已确认：目标与范围

| 决策项 | 结论 |
|--------|------|
| 文档落点 | `docs/problem-lists/2026-09-18-web-chrome-polish/improve-1/` |
| 轮次 | 一轮 `improve-1`；实施分 Stage，不预开 improve-2 |
| 关键改动清单 | **不写**（用户未要求） |
| 工具箭头 | 锁死 **14px**，所有 `ToolPanel` 卡片同一尺寸，不被长摘要挤扁 |
| `>` | **去掉**。不再当终端提示符留在聊天输入框里 |
| 输入垂直对齐 | 单行时占位符和输入文字在框的垂直正中；多行时长高，圆钮仍贴右下 |
| 会话顶栏 | 高度收到约 **44px**（主要减上下内边距） |
| 侧栏顶 | **不改**。不要把会话 header 和侧栏顶对齐；两边底边可以不齐 |
| 其它工具 | 只需修共用 `ToolPanel`；不必给 Tasks / reasoning / 目录箭头单独改尺寸 |
| 权威文档 | 落地后改 `components.md`（去掉 `>`、写明顶栏高度与箭头 14px）；不回写 density / send-stop 的 00–05 |

## 3. 已确认：板式

```
工具折叠行:  [外层白卡]  彩色工具名  摘要…  running  ▾(14px，不被挤)
工具展开:    同上，chevron 旋转，结果 <pre> 不动

输入单行:    [垂直居中的占位/文字] [🧠 unknown?] [○ 纸飞机或方块]
输入多行:    textarea 往上长；圆钮和思考芯片贴右下
输入不再有:  左侧蓝色 `>`

会话顶栏:    [2×2 logo][OHBABY]     running | 模型名 | 圆环 | goal?
             总高约 44px；侧栏顶仍 58px，不对齐
```

## 4. 已确认：边界（不做的事）

| 项 | 本轮不做 |
|----|----------|
| TUI | 零改动 |
| 侧栏顶 `.ohb-sidebar-header`、项目轨 | 用户明确先不改、不要对齐 |
| 空项目页 `.ohb-empty-status` | 不是这条会话 header |
| Tasks / reasoning / 目录选择器箭头 | 不同控件，不借这次统一 |
| 改 1–7 行封顶、`fitComposerTextarea` 常量 | 仍 24px 行高、168px 上限；textarea **自身 padding 保持 0** |
| 发送/排队/pairing/abort | 只改脸上的位置和有没有 `>` |
| 打字机文案 "Ask Lychee" 与品牌不一致 | 另题 |
| 会话流顶部 32px padding | 用户没提 |
| 像素截图回归、真实模型链路 | 本轮不要求；须启动本地 Web，以可重复数据完成浏览器端到端检查 |
| 原规划会话写代码 | 审查通过后再实施 |
| 回写 density 00–05 | 本轮覆盖「`>` 贴顶」；旧文档当历史 |

## 5. 已确认：与关联议题的关系

| 文档 | 关系 |
|------|------|
| [2026-09-18-web-composer-density](../../2026-09-18-web-composer-density/README.md) | 前一轮规定「长高后圆钮贴右下、`>` 贴顶」。本轮：**去掉 `>`**，圆钮贴右下保留。不回写那批 00–05。 |
| [2026-09-18-web-composer-send-stop](../../2026-09-18-web-composer-send-stop/README.md) | 单槽互斥、思考芯片进框。本轮不改互斥。 |
| [`docs/ohbaby-web/ui/components.md`](../../../ohbaby-web/ui/components.md) | 仍写「`>` 提示符」；顶栏未写高度；工具 chevron 未写死尺寸。本轮落地后必须改这篇。 |
| [`docs/ohbaby-web/test.md`](../../../ohbaby-web/test.md) | composer density 行未提「无 `>` / 单行垂直居中」。本轮补一句。 |
| [2026-08-18-web-tool-failure-presentation](../../2026-08-18-web-tool-failure-presentation/) | 工具单卡配对、短失败自动展开。本轮不改配对和展开，只锁箭头几何。 |

## 6. 用户确认记录

- 2026-09-18：工具卡箭头大小不一致，要统一；并检查其它工具是否同类问题。
- 2026-09-18：输入文字（含未输入前占位）偏下，`>` 偏上；建议直接去掉 `>`。
- 2026-09-18：会话框上方 header 高度缩短。
- 2026-09-18：工具卡箭头锁 **14px**。
- 2026-09-18：侧栏顶先不改，**不要**把会话 header 和侧栏顶部对齐。
- 2026-09-18：用 plan-code-improvement 写文档、做文档自检，**先不实施**。

规划期未决项：**无。** 顶栏 44px、箭头 14px、去掉 `>`、侧栏不对齐，按上表写入。

- 2026-09-18：用户再次确认上述方案并授权实施；要求测试、子代理审查、启动本地 E2E 并打开浏览器验证。完成后先不提交，等待用户检查 Web 页面。
